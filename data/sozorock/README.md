# SozoRock County — synthetic demonstration place

Not a real county. FIPS `00000`. Lenses `00000` Seat town, `00001` North ridge, `00002` River mill.

Used so the product can show all twelve barrier dimensions and every act (See, Compare, Plan, Resource, Track, Input, Decision Record) without implying a partnership with any real government.

North ridge → Trust is deliberately Not available so hatch is visible. Missing is not zero.

Replace this seed by opening `apps/table/product.html` → Input county.
