# Planning table

## Product table (open this first)

`product.html` — SozoRock County synthetic seed. All twelve dimensions. All product acts. Any county can type values or import JSON.

Synthetic demonstration county. Not a real place. Not a deployment.

## Lewis reference table

Public-data demonstration. Not a Lewis County deployment.

```
python3 -m http.server 8765 --directory apps/table
```

- Product table: `http://localhost:8765/product.html`
- Lewis 3D: `http://localhost:8765/`
- Lewis 2D: `http://localhost:8765/fallback.html`

## Contract

- Color and hatch = evidence state of the **open** dimension.
- Missing is drawn pale + hatch. Never zero.
- ZIP codes are planning lenses, not labels for people.
- Compare is two published dates. Not a ranking.
- Plan is a labeled scenario. Not a prediction of individual health.
- Decision Record is always an AI draft until a human accepts.
- County input with a blank detail becomes Not available.
