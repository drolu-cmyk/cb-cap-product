{
  "name": "cbcap-platform",
  "private": true,
  "type": "module",
  "version": "0.4.0",
  "description": "CB-CAP edge API — SozoRock Tech Inc.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test worker/*.test.js"
  }
}
